/**
 * Browser-loadable Dogpile SDK bundle entrypoint.
 *
 * This mirrors the public package root so browser import-map and CDN users get
 * the same documented API surface from a bundled ESM artifact.
 */
export * from "../index.js";
