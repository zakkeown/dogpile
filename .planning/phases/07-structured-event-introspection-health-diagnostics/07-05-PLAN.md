---
phase: 07-structured-event-introspection-health-diagnostics
plan: 05
type: execute
wave: 4
depends_on:
  - "07-02"
  - "07-03"
  - "07-04"
files_modified:
  - src/index.ts
  - package.json
  - src/tests/package-exports.test.ts
  - src/tests/event-schema.test.ts
  - CHANGELOG.md
  - CLAUDE.md
autonomous: true
requirements:
  - INTR-01
  - INTR-02
  - HLTH-01
  - HLTH-02

must_haves:
  truths:
    - "AnomalyCode, HealthAnomaly, RunHealthSummary are in the explicit export type list in src/index.ts"
    - "package.json exports contains /runtime/introspection subpath entry"
    - "package.json exports contains /runtime/health subpath entry"
    - "src/tests/package-exports.test.ts asserts both new subpaths exist"
    - "pnpm run verify exits 0 (release gate: build + typecheck + test all green)"
    - "CHANGELOG.md documents result.health on RunResult, two new subpaths, queryEvents API"
    - "CLAUDE.md cross-cutting invariants updated to include Phase 7 public-surface changes"
  artifacts:
    - path: "src/index.ts"
      provides: "AnomalyCode, HealthAnomaly, RunHealthSummary exported from the root public surface"
      contains: "AnomalyCode"
    - path: "package.json"
      provides: "/runtime/introspection and /runtime/health subpath exports"
      contains: "runtime/introspection"
    - path: "src/tests/package-exports.test.ts"
      provides: "Assertions for both new subpaths in the tarball export list"
      contains: "runtime/introspection"
    - path: "CHANGELOG.md"
      provides: "v0.5.0 Phase 7 entry documenting public-surface changes"
      contains: "queryEvents"
    - path: "CLAUDE.md"
      provides: "Updated invariant chain listing Phase 7 public-surface additions"
      contains: "runtime/introspection"
  key_links:
    - from: "src/index.ts"
      to: "src/types.ts"
      via: "explicit export type { AnomalyCode, HealthAnomaly, RunHealthSummary } from ./types.js"
      pattern: "AnomalyCode"
    - from: "package.json exports"
      to: "dist/runtime/introspection.js"
      via: "subpath export pattern"
      pattern: "runtime/introspection"
    - from: "src/tests/package-exports.test.ts"
      to: "package.json exports"
      via: "manifest assertion"
      pattern: "introspection"
---

<objective>
Wire the three new health types into src/index.ts, add two new subpath exports to package.json, and update all public-surface lockstep files in a single plan to prevent drift. This is the final plan of Phase 7 — all implementation is complete; this plan ensures the public API surface is fully declared and documented.

Purpose: STATE.md invariant — "every event/result/exports change updates package-exports.test.ts, result-contract.test.ts, package.json exports/files, and CHANGELOG.md in lockstep." Phase 7 adds two new subpaths, result.health, and three new root-index type exports.

Output: AnomalyCode, HealthAnomaly, RunHealthSummary in src/index.ts; two new package.json subpath exports; all lockstep files updated; pnpm run verify green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07-structured-event-introspection-health-diagnostics/07-CONTEXT.md
@.planning/phases/07-structured-event-introspection-health-diagnostics/07-RESEARCH.md

<interfaces>
<!-- Existing subpath pattern from package.json (for exact format to replicate). -->

Existing subpath export pattern (VERIFIED: package.json):
```json
"./runtime/provenance": {
  "types": "./dist/runtime/provenance.d.ts",
  "import": "./dist/runtime/provenance.js",
  "default": "./dist/runtime/provenance.js"
}
```

New subpaths to add (same pattern, different module names):
```json
"./runtime/introspection": {
  "types": "./dist/runtime/introspection.d.ts",
  "import": "./dist/runtime/introspection.js",
  "default": "./dist/runtime/introspection.js"
},
"./runtime/health": {
  "types": "./dist/runtime/health.d.ts",
  "import": "./dist/runtime/health.js",
  "default": "./dist/runtime/health.js"
}
```

Note: The `files` allowlist in package.json uses glob `"dist/runtime/*.js"` etc. — new files under src/runtime/ are automatically covered by the glob. No files entry change needed.

From src/index.ts (explicit type export block — lines 77–211):
The export type { ... } from "./types.js" block enumerates types alphabetically.
New types to insert in alphabetical order:
  - AnomalyCode — after AgentSpec, before BroadcastContribution
  - HealthAnomaly — after FinalEvent, before FirstOfTerminationCondition
  - RunHealthSummary — after RunEventLog, before RunMetadata
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add AnomalyCode, HealthAnomaly, RunHealthSummary to src/index.ts root export</name>
  <read_first>
    - src/index.ts (lines 77–211 — the export type { ... } from "./types.js" block, to find the three insertion points in alphabetical order)
  </read_first>
  <files>src/index.ts</files>
  <action>
In the large `export type { ... } from "./types.js"` block (lines 77–211), add three entries in alphabetical position:

1. `AnomalyCode` — insert after `AgentSpec` (line ~80), before `BroadcastContribution`:
```typescript
  AnomalyCode,
```

2. `HealthAnomaly` — insert after `FinalEvent` (line ~104), before `FirstOfTerminationCondition`:
```typescript
  HealthAnomaly,
```

3. `RunHealthSummary` — insert after `RunEventLog` (line ~151), before `RunMetadata`:
```typescript
  RunHealthSummary,
```

Do NOT modify any other line. The three types are already defined in src/types.ts (added in plan 07-01). This task only adds them to the explicit re-export list so root callers (`import type { RunHealthSummary } from "@dogpile/sdk"`) can access them.

After editing, run:
`pnpm run typecheck 2>&1 | tail -5`
  </action>
  <verify>
    <automated>cd /Users/zakkeown/Code/dogpile && grep -c "AnomalyCode\|HealthAnomaly\|RunHealthSummary" src/index.ts && pnpm run typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>
    - grep -c "AnomalyCode\|HealthAnomaly\|RunHealthSummary" src/index.ts returns 3
    - pnpm run typecheck exits 0
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add subpath exports to package.json + update package-exports.test.ts</name>
  <read_first>
    - package.json (lines 31–166 — full exports block and files allowlist, to find insertion point and confirm glob coverage)
    - src/tests/package-exports.test.ts (full file — understand how subpath exports are asserted, find the assertion list to extend)
    - .planning/phases/07-structured-event-introspection-health-diagnostics/07-RESEARCH.md (Subpath Export Pattern section)
  </read_first>
  <files>package.json, src/tests/package-exports.test.ts</files>
  <action>
**Change 1: package.json — add two new subpath entries**

In the `"exports"` block, add after the `"./runtime/provenance"` entry (or in the alphabetical position among `./runtime/*` entries):

```json
"./runtime/health": {
  "types": "./dist/runtime/health.d.ts",
  "import": "./dist/runtime/health.js",
  "default": "./dist/runtime/health.js"
},
"./runtime/introspection": {
  "types": "./dist/runtime/introspection.d.ts",
  "import": "./dist/runtime/introspection.js",
  "default": "./dist/runtime/introspection.js"
}
```

Insert both entries in alphabetical order among the existing `./runtime/*` subpaths. Do NOT change the `"files"` allowlist — the glob `"dist/runtime/*.js"` already covers new files. [VERIFIED: package.json]

**Change 2: src/tests/package-exports.test.ts — assert both new subpaths**

Find the existing subpath assertions (the test that checks package.json exports contain expected subpaths). Add `"./runtime/health"` and `"./runtime/introspection"` to the list of expected public subpaths.

Also add import-level type assertions if the test pattern includes TypeScript-level import verification:
```typescript
import type { computeHealth, HealthThresholds, DEFAULT_HEALTH_THRESHOLDS, RunHealthSummary } from "@dogpile/sdk/runtime/health";
import type { queryEvents, EventQueryFilter } from "@dogpile/sdk/runtime/introspection";
```

These imports verify the subpaths are wired and their types are accessible. If the test file uses a different assertion pattern (e.g., checking the manifest object), follow the existing pattern exactly.

After changes:
`pnpm run build && pnpm vitest run src/tests/package-exports.test.ts`
  </action>
  <verify>
    <automated>cd /Users/zakkeown/Code/dogpile && grep "runtime/introspection" package.json | wc -l</automated>
  </verify>
  <done>
    - grep "runtime/introspection" package.json returns >= 3 lines (types, import, default)
    - grep "runtime/health" package.json returns >= 3 lines
    - pnpm run build exits 0 (dist/runtime/introspection.js and dist/runtime/health.js emitted)
    - pnpm vitest run src/tests/package-exports.test.ts exits 0
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update event-schema.test.ts + CHANGELOG.md + CLAUDE.md</name>
  <read_first>
    - src/tests/event-schema.test.ts (full file — understand what it asserts; Phase 7 adds no new event types but RunResult shape changed)
    - CHANGELOG.md (find the v0.5.0 in-progress section or most recent entry to determine insertion point)
    - CLAUDE.md (find the public-surface invariants section that lists per-phase changes; identify where to add Phase 7)
    - .planning/phases/07-structured-event-introspection-health-diagnostics/07-CONTEXT.md (D-10: subpath exports; review full decisions list for changelog content)
  </read_first>
  <files>src/tests/event-schema.test.ts, CHANGELOG.md, CLAUDE.md</files>
  <action>
**Change 1: src/tests/event-schema.test.ts**

Phase 7 does NOT add new event types (no event-shape change per STATE.md invariant). However, RunResult gains a new required `health` field. If event-schema.test.ts asserts the RunResult shape, add an assertion for `health`.

Read the full file first. If it only asserts event type discriminants and event field shapes (not RunResult fields), no change is needed — document the no-op in the SUMMARY. If it includes RunResult field assertions, add:
- `result.health` is present
- `result.health.anomalies` is an array
- `result.health.stats` has `totalTurns`, `agentCount`, `budgetUtilizationPct`

**Change 2: CHANGELOG.md**

Find the v0.5.0 section (or the in-progress unreleased section). Add a Phase 7 entry documenting all public-surface additions. Format must match the existing changelog style.

Content for the entry:

```markdown
### Phase 7: Structured Event Introspection + Health Diagnostics

**New exports:**
- `@dogpile/sdk/runtime/introspection` — `queryEvents(events, filter)` typed event query function.
  Filters a `readonly RunEvent[]` by type, agentId, turn range, and/or cost range with AND semantics.
  Returns a narrowed subtype (e.g., `TurnEvent[]` when `filter.type === "agent-turn"`) — no cast required.
- `@dogpile/sdk/runtime/health` — `computeHealth(trace, thresholds?)` health summary function.
  `HealthThresholds`, `DEFAULT_HEALTH_THRESHOLDS`, `RunHealthSummary`, `HealthAnomaly` types.

**RunResult changes:**
- `result.health: RunHealthSummary` — always-present machine-readable health summary, auto-computed
  from trace events at result time. Re-computed identically by `replay()`. Fields:
  - `health.anomalies: readonly HealthAnomaly[]` — detected anomalies with codes:
    `"runaway-turns"` | `"budget-near-miss"` | `"empty-contribution"` | `"provider-error-recovered"`
  - `health.stats.totalTurns: number`
  - `health.stats.agentCount: number`
  - `health.stats.budgetUtilizationPct: number | null`

**New types exported from `@dogpile/sdk`:**
- `RunHealthSummary`, `HealthAnomaly`, `AnomalyCode`

**Frozen fixture:**
- `src/tests/fixtures/anomaly-record-v1.json` — one sample `HealthAnomaly` per anomaly code.
  Tests reject field changes without explicit fixture update.

**Note:** `provider-error-recovered` is in the `AnomalyCode` union and fixture but is never emitted
by `computeHealth` in Phase 7 — no trace signal exists without an event-shape change (deferred).
```

**Change 3: CLAUDE.md**

Find the cross-cutting invariants section that lists public-surface changes per phase. Add Phase 7 to the list. The exact location depends on the current file structure — read the file first.

Add a note similar to:
```
**Phase 7 (Introspection + Health):** `result.health: RunHealthSummary` added to RunResult (required,
always present); `@dogpile/sdk/runtime/introspection` and `@dogpile/sdk/runtime/health` subpaths added.
Changes propagate to `src/tests/result-contract.test.ts`, `src/tests/package-exports.test.ts`,
`src/tests/health-shape.test.ts`, `package.json` exports, and `CHANGELOG.md`.
`AnomalyCode`, `HealthAnomaly`, `RunHealthSummary` exported from the root index.
```

**Final verification — run the full release gate:**
`pnpm run verify`
  </action>
  <verify>
    <automated>cd /Users/zakkeown/Code/dogpile && pnpm run verify 2>&1 | tail -20</automated>
  </verify>
  <done>
    - src/tests/event-schema.test.ts updated or confirmed no-op (documented in SUMMARY)
    - CHANGELOG.md contains Phase 7 entry with queryEvents, result.health, two new subpaths
    - CLAUDE.md invariant chain includes Phase 7 public-surface additions
    - pnpm run verify exits 0 (full release gate: build + typecheck + test green)
    - grep "runtime/introspection" src/tests/package-exports.test.ts returns >= 1 match
    - grep "queryEvents" CHANGELOG.md returns >= 1 match
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm tarball → consumer | Published subpaths must resolve correctly; test suite enforces this. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09 | Repudiation | subpath export drift | mitigate | package-exports.test.ts asserts both new subpaths in the published tarball. pnpm run verify catches misconfiguration before publish. |
| T-07-10 | Tampering | CHANGELOG omission | accept | CHANGELOG is human-authored documentation. No runtime impact. |
</threat_model>

<verification>
- `pnpm run verify` exits 0 (full release gate)
- `grep -c "AnomalyCode\|HealthAnomaly\|RunHealthSummary" src/index.ts` returns 3
- `grep "runtime/introspection" package.json` returns the types/import/default triplet
- `grep "runtime/health" package.json` returns the types/import/default triplet
- `pnpm run build` produces `dist/runtime/introspection.js` and `dist/runtime/health.js`
- `grep "queryEvents" CHANGELOG.md` returns >= 1 match
- `grep "result.health" CHANGELOG.md` returns >= 1 match
</verification>

<success_criteria>
- AnomalyCode, HealthAnomaly, RunHealthSummary in src/index.ts explicit export type block
- Both new subpaths in package.json exports with correct types/import/default triplet
- package-exports.test.ts asserts /runtime/introspection and /runtime/health subpaths
- CHANGELOG.md documents result.health, queryEvents API, two new subpaths, AnomalyCode note
- CLAUDE.md invariant chain updated with Phase 7 additions
- pnpm run verify exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/07-structured-event-introspection-health-diagnostics/07-05-SUMMARY.md`
</output>
